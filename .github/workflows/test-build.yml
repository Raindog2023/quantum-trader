name: Test Docker Build

on:
  pull_request:
    branches: [main]
  push:
    branches: [develop]

jobs:
  test-build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Build Docker image
        run: |
          docker buildx build \
            -f Dockerfile.safe \
            -t quantum-trader-safe:test \
            --load \
            .
      
      - name: Start services
        run: |
          docker-compose -f docker-compose.safe.yml up -d
          sleep 10
      
      - name: Check container health
        run: |
          docker compose -f docker-compose.safe.yml ps
          docker inspect r20-quantum-trader-safe
      
      - name: Test API endpoint
        run: |
          curl -f http://localhost:8080/api/v1/health || exit 1
          echo "✅ API is responding"
      
      - name: Check logs
        if: always()
        run: |
          docker-compose -f docker-compose.safe.yml logs
      
      - name: Cleanup
        if: always()
        run: |
          docker-compose -f docker-compose.safe.yml down -v

  safety-check:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Run safety validator
        run: |
          python scripts/safety_validator.py
        env:
          TRADING_MODE: backtest
          DRY_RUN: "1"
          STRATEGY_VALIDATION: "1"
